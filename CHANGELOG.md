# Changelog

All notable changes to this project are documented via squash-merged PR
titles in `git log` (conventional-commit style: `type(scope): description`).
This file begins tracking human-readable release notes going forward;
history prior to this point can be reconstructed from `git log --oneline`.

## Unreleased

- Started this changelog.
